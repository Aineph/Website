{% extends 'base.html.twig' %}
{% block title %}
    {{ 'admin.index.title' | trans }}
{% endblock %}
{% block body %}

    <!-- Admin Index Body -->

    <div class="container">
        <div class="row mt-5">

            <!-- Admin Index Users Management -->

            <div class="col-12 col-md-6 p-3">
                <div class="card text-center element-grow">
                    <div class="card-body">
                        <img src="{{ asset('build/admin/index/user.svg') }}"
                             alt="{{ 'admin.index.users' | trans }}" width="50px"
                             height="50px"/>
                        <div class="card-text">{{ 'admin.index.users' | trans }}</div>
                        <a class="stretched-link" href="{{ path('admin_users') }}"></a>
                    </div>
                </div>
            </div>

            <!-- Admin Index Messages Management -->

            <div class="col-12 col-md-6 p-3">
                <div class="card text-center element-grow">
                    <div class="card-body">
                        <img src="{{ asset('build/admin/index/message.svg') }}"
                             alt="{{ 'admin.index.messages' | trans }}" width="50px"
                             height="50px"/>
                        <div class="card-text">{{ 'admin.index.messages' | trans }}</div>
                        <a class="stretched-link" href="{{ path('admin_messages') }}"></a>
                    </div>
                </div>
            </div>

            <!-- Admin Index Articles Management -->

            <div class="col-12 col-md-6 p-3">
                <div class="card text-center element-grow">
                    <div class="card-body">
                        <img src="{{ asset('build/admin/index/article.svg') }}"
                             alt="{{ 'admin.index.articles' | trans }}" width="50px"
                             height="50px"/>
                        <div class="card-text">{{ 'admin.index.articles' | trans }}</div>
                        <a class="stretched-link" href="{{ path('admin_articles') }}"></a>
                    </div>
                </div>
            </div>

            <!-- Admin Index Missions Management -->

            <div class="col-12 col-md-6 p-3">
                <div class="card text-center element-grow">
                    <div class="card-body">
                        <img src="{{ asset('build/admin/index/task.svg') }}"
                             alt="{{ 'admin.index.tasks' | trans }}" width="50px"
                             height="50px"/>
                        <div class="card-text">{{ 'admin.index.tasks' | trans }}</div>
                        <a class="stretched-link" href="{{ path('admin_missions') }}"></a>
                    </div>
                </div>
            </div>

            <!-- Admin Index Projects Management -->

            <div class="col-12 col-md-6 p-3">
                <div class="card text-center element-grow">
                    <div class="card-body">
                        <img src="{{ asset('build/admin/index/project.svg') }}"
                             alt="{{ 'admin.index.projects' | trans }}" width="50px"
                             height="50px"/>
                        <div class="card-text">{{ 'admin.index.projects' | trans }}</div>
                        <a class="stretched-link" href="{{ path('admin_projects') }}"></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
