{% block profile_footer %}

    <!-- Profile Footer -->

    <div class="container-fluid">
        <div class="row mt-5">

            <!-- Profile Footer Actions -->

            <div class="col-12 col-md-6 p-3 text-center">
                <a class="btn btn-lg btn-outline-light" href="{{ path('profile_edit') }}">
                    {{ 'profile.footer.edit' | trans }}
                </a>
            </div>
            <div class="col-12 col-md-6 p-3 text-center">
                <a class="btn btn-lg btn-outline-danger" href="{{ path('security_logout') }}">
                    {{ 'profile.footer.logout' | trans }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}
